# 模型清单

**这一页只写 checkpoint 里读不出来的东西**：每份数据当时在录什么、哪个模型能用、
哪个不能用、以及为什么。能从 checkpoint 里读出来的部分不要手抄，会抄错也会过期：

```powershell
.\.venv-win\Scripts\python.exe tools\policy\list_models.py
```

它直接读 `policies/*/config.json` 和 `train_config.json`，永远和磁盘上的一致。

---

## 一句话先看这个

| 想干什么 | 用哪个 |
|---|---|
| 测**指令跟随**（说红色就抓红色） | 只能用 `smolvla_*`。ACT 没有语言输入 |
| 只测把黄色方块放进碗 | `act_v3rand_100k`，目前最好的单任务模型 |
| 带 `_OLDCAMMAP` 的 | **不要用**，相机映射修正之前训的，会喂进对调的画面 |

**ACT 完全没有语言输入。** `modeling_act.py` 里 task / language / text 一共出现一次，
在 Apache 许可证文本里。所以不管你在面板上按哪个按钮、打什么字，
ACT 模型只会做它那份数据里的那一件事。
2026-09-07 晚上"我要它把方块拿出来，它却往碗里放"就是这么来的：
当时加载的是 `act_v3rand_100k`。

---

## 数据集

| 数据集 | 条数 | 帧数 | 录的是什么 | 指令 |
|---|---|---|---|---|
| `so101_pickplace` | 30 | 14332 | 最早的 30 条，黄色方块放进碗 | 1 条 |
| `so101_v2` | 75 | 29087 | 同上，扩到 75 条 | 1 条 |
| `so101_v3` | 130 | 61261 | 同上，130 条，位置更分散 | 1 条 |
| `so101_color6` | 150 | 76358 | 从碗里**拿出**指定颜色，5 色 × 30，粉色留作没训过的对照 | 5 条 |
| `so101_mix1` | 280 | 137619 | 上面两份合并，两个方向都有 | 6 条 |

指令原文：

```
Pick the yellow block and put it in the black bowl.          <- so101_v3 系列
Pick the {red|blue|yellow|green|purple} cube out of the bowl and put it on the table.
```

注意 **yellow 在两个方向里都出现**，方向相反。这是故意的：
policy 不能靠"看到 yellow 就往碗里放"蒙混过去，必须读完整句话。

`*_trim` 后缀是处理步骤不是另一份录制：切掉开头静止的那一段。
早期 22% 的帧在教模型发呆（README 第 27 条）。

---

## 命名约定

```
<算法>_<数据集>_<步数>
smolvla_mix1_20k      SmolVLA，so101_mix1，20000 步
act_v3rand_100k       ACT，so101_v3，100000 步，rand = 续训自随机化位置那一版
smolvla_v3only_20k    SmolVLA，只有 so101_v3
smolvla_color6only_20k SmolVLA，只有 so101_color6
```

`only` 后缀专门表示**单任务对照组**：只见过一个方向的数据。
没有 `only` 的 SmolVLA 都是合并数据训的。

---

## 相机键名

两套约定，不能混：

| 模型 | 期望的键 |
|---|---|
| ACT（本地训练） | `observation.images.top` / `.wrist` |
| SmolVLA（从 `lerobot/smolvla_base` 微调） | `camera1` / `camera2` / `camera3` |

`smolvla_base` 是在 481 个社区数据集上预训练的，它的键名是那边的约定，
改不了。所以训练和部署两侧都要 `--rename_map`。
`robot.ps1 eval` 会调 `camera_rename.py` 从 checkpoint 自己的 config 里推出这个映射，
不用手填 —— 填错了它会静默地什么都不改，然后画面对调。

SmolVLA 只有两个相机，第三个由框架补 `-1` 图像加零掩码，这是它本来就支持的。

---

## 各个模型

运行 `list_models.py` 看权威表格。这里只记它读不出来的判断：

### 在用的

- **`act_v3rand_100k`** — 单任务里最好的一个，位置泛化能做到。
  续训自 `act_so101_v3_trim_20260901_1854`。**不听指令。**
- **`smolvla_mix1_20k`** / **`smolvla_mix1_100k`** — 同一份配置，只差步数
  （`local/so101_mix1_trim`、`smolvla_base`、seed 1000、batch 16）。
  是一组干净的对照：唯一变量是训练步数。100k 那个最终 loss 0.025，训了 9 小时 21 分；
  20k 那个 1 小时 53 分。
  **这两个是否真的听指令，到现在没有可信的测量。**
  之前两次尝试的测量都是无效的，原因写在下面。

### 2026-09-08 训的两个对照组（已完成）

| 模型 | 数据 | 最终 loss | 用时 |
|---|---|---|---|
| `smolvla_v3only_20k` | so101_v3，只见过"放进碗" | 0.115 | 81 分钟 |
| `smolvla_color6only_20k` | so101_color6，只见过"拿出来" | 0.104 | 71 分钟 |

v3 那个在第 8000 步被 `CUDA error: unknown error` 打断过一次，
从 checkpoint 8000 续训完成，所以比另一个多用了十分钟。
这正是重试逻辑该起作用的场合 —— 早上那个版本会把它直接判死。

为什么要训这两个：混合模型如果表现不好，有两个说不清的原因 ——
可能是它没学会读指令，也可能是它连单个任务本身都没学会。
混着训的模型分不开这两件事。这两个各自只见过一个方向，
所以它们回答的是"这个任务本身能不能做成"，是混合模型的下限参照。

两者之间**只有数据集不同**：同一个 base、同一个 rename、同样 20000 步、同样 batch 8。
所以它们之间的差别只来自数据。

**但它们和 `smolvla_mix1_*` 不严格可比：mix1 用的是 batch 16，这两个是 batch 8。**
不是选择，是被迫的。2026-09-08 batch 16 连续三次在第一次前向传播里挂掉，
死在 SmolVLA 的 eager attention 的 softmax 上，申请 48 MiB 而当时空闲 4.84 GiB，
同时报告自己占用了 17179869184 GiB —— 正好是 2^64 字节，
一次无符号减法回绕，因为驱动返回的 free 比 total 还大。
环境是 torch 2.11.0+cu130、驱动 581.95、跑在 WSL 里。
`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 试过，无效。
batch 8 干净地跑起来了，4.4 step/s，占用 2.96 GB。
学习率等其余参数一律不动，只改被迫改的那一个。

如果以后要三者严格可比，就再用 batch 8 训一个 mix1，约 75 分钟。

### 不要用

- **`act_pickplace_100k_OLDCAMMAP`**、**`act_trim_100k_OLDCAMMAP`** —
  相机映射修正之前训的，两路画面是对调的。留着只为记录。
- **`act_v2_100k`**、**`act_v2_chunk50_100k`**、**`act_v3_100k`** — 上一版，留作对比。

---

## 2026-09-09 第一批实机结果

| 模型 | 放进碗 | 从碗里拿出来 |
|---|---|---|
| `smolvla_v3only_20k` | **能抓**，但比 ACT 抖很多、慢很多 | 没这个数据 |
| `smolvla_color6only_20k` | 没这个数据 | **不会**，不朝任何方块伸手，抓空气 |
| `smolvla_mix1_100k` | **能抓** | **不会** |

两个单任务对照组正是为分辨这件事训的，它们给出了干净的答案：
**"从碗里拿出来"的失败不是指令跟随的问题。**
只见过这一个方向的 `color6only` 同样做不到，而且它连"朝某个方块伸手"都没有，
所以不是分不清颜色，是这个动作本身没有形成。
另外混合训练**没有破坏**已经学会的"放进碗"。

### 关于速度的一段分析，作废

写过一整段说控制循环只有 1.9 Hz、30 秒只走完一条示范的 12%、结果可能全部作废。
**那是错的**，收回。

`Record loop is running slower (X Hz)` 这条警告只在**某一拍超时**时打印，
报的是**那一拍**的速率，不是平均值。整个会话只有 32 拍超时，其余的拍都快到要 sleep。

真实情况：

| | |
|---|---|
| 动作块 50 步 @ 30fps | 1.67 秒放完 |
| SmolVLA 重新规划一次 | 486 ms（实测；ACT 是 25 ms）|
| 两者相加 | 约 2.2 秒 |
| 日志里警告的实测间隔 | 约 2 秒 |

**循环一直是正常的 30 fps，只是每隔约两秒有一拍停半秒去重新规划。**
这就是"游游弋弋"的全部来源，而且是 SmolVLA 固有的，与 rerun 无关 ——
实测开不开 display 没有差别，待命时段一条警告都没有。

想消掉这个顿挫，lerobot 有异步推理引擎（`--inference.type=rtc`），
把重新规划放到后台线程。还没接。

### `smolvla_c6pool_20k` — 颜色无关的合并模型（2026-09-09 03:10 训完）

02:00 开始，03:10 结束，70 分钟，最终 loss **0.105**
（`v3only` 0.115、`color6only` 0.104，三者同一配置，只差数据）。

数据是 `so101_c6pool_trim`：
`so101_color6_trim` 的全部 150 条，五条指令合并成一条

```
Pick a cube out of the bowl and put it on the table.
```

**为什么这么做**：150 条其实是同一个动作被拆成 5 个指令、每个只有 30 条。
合并之后动作有 150 条，回答的是"这个动作本身学不学得会"，抓到哪个方块都算成功。

**它故意是有歧义的**：一幅画面对应 5 个正确答案。
回归型策略会把它们平均掉、伸向碗中间抓空气 —— 那正是观察到的症状；
流匹配的 SmolVLA 应该挑一个模态。而且如果模型本来就没在用语言，
这个任务对它早就是歧义的，合并只是把它摆到明面上。

| 结果 | 说明 |
|---|---|
| 抓住任意一个方块 | 动作 150 条学得会，问题在颜色条件化 |
| 还是抓空气 | 动作本身难，或者 150 条也不够 |

配置和 09-08 那两个完全一致：同一个 base、同一套 rename、batch 8、20000 步。
唯一变量是数据。

`tools/dataset/pool_tasks.py` 做的数据集，做完当场验过：
150 条全在、59055 帧、`task_index` 全为 0、
用训练器自己的读法抽查五个位置拿到的都是同一句指令、preflight 通过。

### 所以第一批结果是有效的

速度不是干扰项，上面那张表可以照读。下一个要查的是数据量：

| 数据集 | 每种行为的示范条数 |
|---|---|
| `so101_v3` | 130 条，全是同一个动作 |
| `so101_color6` | **每种颜色只有 30 条** |

这台机器上 30 条曾经就是不够的 —— 最早的 `so101_pickplace` 就是 30 条，
一路加到 75、130 才成功。

---

## 关于"这些模型到底听不听指令"

**到目前为止没有一个可信的答案。** 试过两次，两次都无效，写在这里免得再犯：

1. 拿 `predict_action_chunk` 的输出（**归一化后的**，没过 postprocessor）
   去比 parquet 里的动作（**没归一化的关节角**）。误差 33 到 51，而动作本身的尺度是 0.7 到 1.2。
2. 在 `t = len(episode)//2` 采样 —— 那是**搬运阶段**，所有指令产生的动作都一样。
   一个完美的模型在那个时刻也只能得到随机水平的分数。

一个有效的测量至少要满足四条：动作在同一个空间里比（过 postprocessor）、
采**伸手阶段**而不是搬运阶段、每条指令 20 到 30 次以上、
**并且这个指标先在已知答案的数据上验过**。
最后一条最重要，也是两次都跳过的那一条。

实机测试用面板：一次按钮一次 30 秒的 run，跑完自己停，
中间不能打断（README 第 32g-3 条）。

---

## 09-12：10 万步跑完，以及"为什么颜色不起作用"有答案了

`smolvla_color6_304_100k`：304 条、五条颜色指令、100000 步、batch 8，
00:30 开始 06:17 结束，loss **0.108**（20k 那个是 0.129）。
除步数外与 `smolvla_color6_304_20k` 完全同配置，所以唯一变量是步数。

### 一、抖动：40k 步就到头了

每 2000 步的 checkpoint 全都留着，所以这条曲线不用重训，直接量。
`tools/policy/tremor.py`，48 条计划，括号里是标准误：

| 步数 | `color6_304`（batch 8） | `mix1`（batch 16） |
|---|---|---|
| 20k | 1.608 ±0.027 | 1.391 ±0.029 |
| 40k | 1.389 ±0.021 | 0.859 ±0.020 |
| 60k | 1.390 ±0.021 | 0.808 ±0.020 |
| 80k | 1.374 ±0.019 | 0.807 ±0.020 |
| 100k | 1.362 ±0.019 | 0.724 ±0.021 |

`color6_304` 在 40k 步就平了，后面 6 万步只从 1.389 走到 1.362。
两个数据集停在完全不同的高度：1.37 对 0.80。这个高度是数据定的，不是预算定的。

**同一个 checkpoint 量两次会差 0.1**（`color6_304_20k` 先量到 1.520、后量到 1.623），
所以 tremor.py 现在每次都给误差棒，16 条计划的单个数字不能用来比两个模型。

### 二、颜色词：五倍步数，一点没变

`tools/policy/reads_the_word.py`：比值 20k 是 0.27、100k 是 0.32
（同一个 20k 模型在不同批次量到过 0.36、0.26、0.27，所以这两个数没有差别）。
1.0 才代表词起作用。

`tools/policy/knows_which_cube.py` 换了个更硬的问法：
给它某一条真实 episode 中间的真实画面，看它计划的是**这条** episode 接下来的动作，
还是一个放在哪条上都差不多的通用动作。真值就是录下来的动作，不需要检测器也不需要机械臂。
问的时刻不是拍脑袋选的，是先在数据里找"各条 episode 的未来彼此差别最大"的那一点（45%）。

| 条件 | 自己的未来排在前面 | 排第一 | 比别人近 |
|---|---|---|---|
| 只看关节角（无模型对照） | 83.5% | 10.0% | 13.03 |
| 原样 | 99.9% | 95.0% | 23.25 |
| 换成别条 episode 的画面 | 93.5% | 42.5% | 16.92 |
| 换成错的颜色词 | 99.9% | 97.5% | 23.01 |

50% 是随机水平。**画面是在用的**（换掉画面，排第一从 95% 掉到 42.5%）；
**颜色词完全没在用**（给错的词，结果一模一样）。

### 三、为什么不用 —— 因为数据从来不需要它

`tools/dataset/word_is_redundant.py`，全程不加载模型，
对 304 条每一条问：光凭现在的关节角，能不能认出自己接下来 50 步？
光凭颜色标签呢？

| | 5% | 10% | 20% | 30% | 40% | 50% | 60% | 70% |
|---|---|---|---|---|---|---|---|---|
| 各条未来的差异（单位） | 5.7 | 5.9 | 13.4 | 19.8 | 18.8 | 18.8 | 20.2 | 20.4 |
| 只凭关节角 | 98.7% | 96.2% | 71.8% | 86.9% | 84.8% | 93.4% | 90.8% | 83.9% |
| 只凭颜色标签 | 52.2% | 52.2% | 55.2% | 52.3% | 50.6% | 50.1% | 50.8% | 53.1% |

颜色标签在任何时刻都是随机水平。手臂自己的姿态在任何时刻都接近满分。

再看各条 episode 之间"指令位姿"的差异随时间怎么长起来：

```
  joint               0%    5%   10%   15%   20%   25%   30%   40%   50%
  shoulder_pan.pos   5.9   5.9   5.9   5.8   7.6  11.3  14.6  12.5   8.8
  shoulder_lift.pos  3.0   3.0   3.0   3.1   9.7  24.7  39.0  33.9  22.6
  elbow_flex.pos     2.5   2.5   2.5   3.4  12.3  26.0  37.0  33.1  26.6
```

**前 15% 所有 episode 一模一样**，20% 才开始分叉。
等到动作开始分叉的时候，手臂的姿态已经把答案说出来了。

所以整条因果链是（每一环都量过）：

1. 所有示范开头一段是共同动作，到 20% 才分叉
2. 分叉的时候，手臂自己的姿态已经能预测自己的未来（72–99%）
3. 颜色标签在任何时刻都预测不了任何东西（50–55%）
4. 于是 loss 从来不要求读那个词 —— 模型就不读（给错词，计划一字不变）
5. 画面是在用的，但用法是"顺着眼前这个场景往下走"，不是"挑出被点名的那个"
6. 实机从 home 起步时没有任何东西指定目标，手臂只能滑进它当前姿态最像的那条轨迹
   —— **随机抓一个方块**

这解释了观察到的现象，也说明**加步数和加同样形状的数据都不可能修好它**：
步数已经证伪（五倍步数，比值 0.27→0.32），
数据量也不是问题，冗余是结构性的。

### 四、可以修，但要改数据的形状（没有启动任何东西）

| 做法 | 代价 | 为什么可能有用 |
|---|---|---|
| 从 home 直接伸向被点名的方块，不要先抬再找 | 重录 | 让第一秒的动作就随颜色不同，词才成为唯一信息源 |
| 方块摊在桌上而不是挤在一个碗里 | 重录 | 现在 `shoulder_pan` 全程最多只差 14.6 个单位，物理上的区分度本来就小 |
| 解冻 VLM（`train_expert_only=False`） | 一次训练 | **不推荐先试**：瓶颈是梯度信号不要求读词，不是表达能力不够 |

一个可以顺手验证的预言：既然颜色词根本没在用，
`smolvla_c6pool_304_20k`（五条指令合并成一条）上机械臂应该和
`smolvla_color6_304_20k` 表现一样。不一样就说明上面某一环错了。

### 五、补测：两个相机各自在什么时候能看见东西

"它是靠 wrist camera 抓的"这个猜测,分开量了。
`tools/policy/knows_which_cube.py` 现在把两个相机分别换掉:

| 条件 | 排第一 |
|---|---|
| 原样 | 95.0% |
| 只换顶上那个 | 67.5% |
| 只换 wrist | 60.0% |
| 两个都换 | 40.0% |
| 换错的颜色词 | 95.0% |

两个相机都在用,wrist 稍重一点。词还是零。

`tools/dataset/when_can_it_see.py`,60 条 episode,不分颜色只看饱和度,
问的是"这一帧里有没有方块":

| 相机 | 0% | 10% | 20% | 30% | 40% | 50% | 60% |
|---|---|---|---|---|---|---|---|
| 顶上,画面里方块占比 | 1.3% | 1.3% | 1.5% | 1.5% | 1.0% | 0.7% | 0.7% |
| wrist,画面里方块占比 | 0.0% | 0.0% | 0.0% | 2.3% | 9.5% | 13.8% | 18.4% |
| wrist 整个画面是空的 | **93%** | **93%** | 82% | 32% | 3% | 2% | 0% |

**开局 93% 的 episode 里,wrist camera 里一个方块都没有**,只有白桌面和夹爪。
要到 30% 才开始看见,40% 才看全。而动作在 20% 就已经分叉了。

所以"wrist camera 正对着的就是要抓的东西"这句话是对的,
但它是**人已经选完了之后**的结果,不是选择的原因。
网络学到的是"朝 wrist 画面正中那个东西过去",
这在训练数据上是满分解法,而且完全不需要懂颜色。

顶上那个相机全程都看得见方块,方块边长 44 px,
过 512 的 padding resize 之后 35 px,约 2.2 个 patch —— **分辨率不是瓶颈**。
瓶颈是:该做选择的那一刻,唯一看得见的相机是顶上那个,
而那一刻的动作在所有 episode 里都一样,loss 对选错选对没有区别。

所以修法要动的是时序,不是数据量:
让手臂从 home 直接朝被点名的方块伸过去,不要先抬后找;
或者把 home 位姿改成 wrist camera 一开始就对着碗。
两者都让"选哪个"这件事发生在有信息、且动作立刻不同的时刻。

---

## 09-12 下午：上面第五节的解释是错的，真正的原因是归位归错了数据集

**先撤回**：第五节把"wrist camera 开局看不见方块"当成失败的解释。
这站不住。wrist camera 开局看不见是 FOV 决定的，本来就该这样；
两段式本来就是对的工作方式 —— 顶上相机给粗定位，走过去之后 wrist 接管做精调。
消融的数字跟这个流程并不矛盾。那一节只能说明"选择只能发生在开局"，
不能说明"所以它失败了"。

### 真正的 bug

`logs/session_20260912_103745.log` 第 24 行：

```
no local dataset for local/so101_color6_304_trim; homing with datasets\so101_v2
```

`robot.ps1` 从 checkpoint 里读出训练数据叫 `local/so101_color6_304_trim`，
去掉 `_trim` 得到 `so101_color6_304`，本地只有 `so101_color6`，
匹配不上 —— 然后**回退到了硬编码的 `datasets\so101_v2`**。

| 关节 | so101_color6 起始位姿 | 实际归位到 | checkpoint 训练时见过的范围 |
|---|---|---|---|
| shoulder_pan | 16.5 | 23.6 | -41.5 .. 45.7 |
| wrist_flex | 71.8 | 67.2 | 25.1 .. 99.3 |
| **wrist_roll** | **-66.0** | **+18.2** | **-167.8 .. -8.5** |

wrist_roll 差 84 个单位，而且 +18.2 **在这个 checkpoint 整个训练集的取值范围之外**，
超出上界 27 个单位。**wrist camera 就装在 wrist_roll 上**，
所以每一次 rollout，它看这个场景的角度都是所有示范里从来没出现过的。

这个 bug 之前发生过一次，代码注释里就记着：
"home.py defaulted to the v1 dataset, so every v2 evaluation began 117 units
away in wrist_roll"。同一个关节，同一个机制，换了个入口又犯了。

### 代价有多大

`tools/policy/wrong_home_cost.py`，24 条 episode 的真实开局画面，
只改状态不改画面（这是低估，真机上 wrist 画面也是转的）：

| | 单位 |
|---|---|
| 错误起始位姿让计划偏移 | **13.15** |
| 模型自己的采样噪声 | 3.19 |
| 两条不同 episode 的计划之间的距离 | 8.23 |
| 比值 | **1.60** |

手臂每次起步的位置，比"两条不同示范之间的差别"还要远 1.6 倍。

### 这让哪些结论作废

作废的只有**真机上跑 304 系列模型**的观察 —— 抓空气、抓不准、不回中，
这些都是在错误起始位姿下看到的，不能拿来评价模型。

**不受影响**的是所有离线测量，它们的状态是从数据集里读的，没经过归位：
抖动曲线、颜色词比值 0.27/0.32、给错颜色词计划一字不变、
颜色标签在数据里全程是随机水平。所以"它不读颜色词"这条仍然成立。

### 修了什么

1. `robot.ps1` 的数据集解析现在会逐级去掉结尾的 `_<数字>`，
   `so101_color6_304` → `so101_color6`。
2. **没有回退默认值了**。找不到就停，并说清楚找了哪些名字。
   归位到别的任务的位姿，看起来像成功，这比不归位危险得多。
3. 新增 `-HomeDataset`，给 c6pool 这种"本地没有同名副本"的模型显式指定。
   `panel.py` 有对应的 `--home-dataset`。
4. 新增 `tools/policy/pose_in_range.py`：**不靠名字**，
   直接读 checkpoint 自己 normalizer 里的 `observation.state` min/max，
   检查起始位姿是否落在这个模型真正见过的范围内，越界就 exit 1。
   `robot.ps1` 在动手臂之前调用它，不通过就拒绝运行。
   拿 09-12 那个错误组合验过：确实报 wrist_roll 越界 27 个单位并拒绝。
   全部 14 个 checkpoint 跑过一遍，12 个 OK，两个 c6pool 因为没有本地副本而拒绝。

### 顺带

`live_task.py` 现在每 100 个 tick 报一次**真实循环频率**，以及
"按这个频率跑完一个 50 步的 chunk 要多久，对比训练时的 1.7 秒"。
lerobot 自带的 "running slower" 警告是单 tick 的而且有限流，
09-12 那段日志里 83 秒内只有 15 条、中位数 2.0 Hz，
但那段时间 GPU 上正跑着离线测量，所以那个数字不能拿来定性 —— 现在有真的了。

相机重命名查过了，没有错配：训练和推理都是 top→camera1、wrist→camera2，
checkpoint 的 preprocessor 里也是这个 map。
社区反复提到训练和推理的相机顺序必须一致，这一条我们是对的。

---

## 09-12 实机结果：两个模型确认能用

归位 bug 修好之后测的。这两个的训练数据都叫 `so101_v3_trim`，
本地有同名的 `datasets\so101_v3`，所以**它们本来就没被那个 bug 影响过** ——
这里记的是干净的结果。

| checkpoint | 任务 | 结果 |
|---|---|---|
| `act_v3rand_100k` | 黄块放进黑碗 | **能完成。动作干脆** |
| `smolvla_v3only_20k` | 黄块放进黑碗 | **能完成。但抖，而且做完之后还在动** |

两条都是本人在机械臂上看到的，不是离线指标。

跑法：

```
.\robot.ps1 eval -Policy policies\act_v3rand_100k  -Duration 30
.\robot.ps1 eval -Policy policies\smolvla_v3only_20k -Duration 30
```

ACT 没有语言输入，面板对它没有意义，直接 eval 就行。

### 两个待解释的现象

**抖** —— 已经量过，是计划自身的粗糙度，不是换 chunk 时的顿挫：
`v3only_20k` 1.13，`mix1_100k` 0.68。同样的数据从 2 万步到 10 万步能减半。
所以 `smolvla_mix1_100k` 应该明显更稳，它也会做这个任务，值得直接对比。

**做完之后还在动** —— 原因还没确定。已经排除掉一个：
不是"录到一半就切断"。数据里 `so101_v3` **100%** 的 episode
最后半秒手臂是完全静止的，`so101_color6` 94%、`so101_mix1` 98%。
停止这个行为是教过的。

`tools/policy/does_it_stop.py` 在测下一步：
拿真实 episode 的最后一帧问模型打算走多远，
用同一条 episode 的中段做对照。终点那里示范是停着的，
所以模型在那里规划出来的任何位移都是它自己编的。还没跑完。

### "做完之后还在动"：原因找到了

`tools/policy/does_it_stop.py`，so101_v3 的 20 条 episode，每处 6 次采样，
chunk 50。travel 是一个 chunk 里指令位姿走过的路程，取位移最大的那个关节：

| `smolvla_v3only_20k` | 模型规划 | 示范实际做的 |
|---|---|---|
| 在最后一帧 | **46.7** | **0.0** |
| 在中段 | 71.0 | 35.5 |

示范在终点是停着的，模型却规划了 46.7 单位的位移 —— 是它中段规划量的三分之二。
这段动作完全是它自己编的。

（顺带：中段模型规划 71.0 而示范只走 35.5，规划量是示范的两倍，
和粗糙度那条对得上 —— 它的计划本身在绕路。）

**为什么**。把每条 episode 的首帧和末帧位姿摆在一起：

| 关节 | 开始 | 结束 | 差 |
|---|---|---|---|
| shoulder_pan | 20.5 | 20.3 | 0.3 |
| shoulder_lift | -99.6 | -99.6 | 0.0 |
| elbow_flex | 93.6 | 93.6 | 0.0 |
| wrist_flex | 63.1 | 63.6 | 0.5 |
| wrist_roll | 5.4 | 5.4 | 0.0 |
| gripper | 0.7 | 0.7 | 0.0 |

最大差 0.5 个单位，而不同 episode 之间开局本身就相差 11.7 个单位。
**做完之后手臂回到的位置，和开工前的位置是同一个。**

所以从关节角看，"做完了"和"准备开始"是同一个状态，
训练数据里这个状态 100% 的含义是"开始干活"。
唯一的区别在画面里：方块在碗里还是在桌上。
而我们已经量过这个模型很吃关节角。于是它就又开始干一遍。

这和颜色词那件事是同一个机制：**关节角已经给出答案的地方，模型就不看别的**。

不是 bug，是示范的结构决定的。真要治，要么录一个明显不同的收工位姿，
要么在外面判断任务完成并停掉策略 —— 面板的定长 trial 现在就是在做后者。

`so101_mix1` 本地只有 data 和 meta、没有 videos，所以这个测量在它上面跑不了；
脚本现在会当场说明并跳过，而不是跑了八分钟再崩。

---

## 09-12 晚：怎么看出它瞄的是哪个方块

模型没有检测头，不会输出框，所以"它认出了哪个"读不出来。能读的是**它瞄了哪个**。

### 先走错一条

第一版读的是**结果**：哪个方块最后被挪走了。这条废了 ——
抓空的时候没有任何方块动，它分不出"选错了"和"选对了但没抓住"，
而没抓住恰恰是现在最常见的情况。意图在动作里，不在结果里。
`tools/policy/which_cube_moved.py` 已删除。

### 现在的读法

`tools/policy/aimed_at.py`：**夹爪第一次闭合的那一刻，wrist 画面里哪个颜色离指间最近。**
抓没抓到都不影响，那一帧里写着它瞄的是谁。示范里夹爪开合两次，
抓一次、放到桌上一次，取第一次。

在示范上验证（答案已知）：

| 30 条 episode | |
|---|---|
| 瞄的就是被点名的那个 | **22** |
| 瞄了别的 | **0** |
| 判不了，弃权 | 8 |

**开口就没错过，错的时候它不开口。**弃权 8 次是因为选到的帧里最近的方块
超过一个方块宽度，那种情况下说它瞄了谁是碰运气。

### 沿路修掉的三个检测器错误，三个都是"看起来对"

1. **一个饱和度阈值卡所有颜色**。蓝色 S=177，紫色 68，粉色 26。
   阈值 90 找到四个方块，输出看着很正常 —— 直到把图打开。现在每个颜色各自定。
2. **面积上限按顶视图定**。顶视图里方块 44 px，wrist 视角里 250 px，
   全被上限滤光，只剩远处一片蓝色碎片，读成"它瞄向了别处"。现在按视角传参。
3. **蓝紫按色相切**。顶视图里紫色 H=130，同一个方块在 wrist 视角下 H=119，
   落进蓝色区间。于是夹爪正压在紫色方块上的那一帧，紫色没被检测到。
   蓝和紫真正的区别是鲜艳度不是色相，现在按饱和度分。

顺带：`box_cubes.py` 可以把任意一帧的六个方块框出来、标出被点名的那个，用来肉眼核对。

### 拿它去看模型

```
.\robot.ps1 eval -Policy policies\smolvla_color6_304_20k -Record -Trials 5 -Duration 40 `
    -Task "Pick the red cube out of the bowl and put it on the table."
.\.venv-win\Scripts\python.exe tools\policy\aimed_at.py rollout_probe -out logs\aimed
```

`-Record` 录到 `datasets\rollout_probe`，每次覆盖。

**`-Record` 和 `-Live` 现在不允许一起用**：录制会给整个数据集贴同一条指令标签，
取自 `-Task`，而实时模式中途会换指令 —— 录出来的标签对哪一段都不成立，
事后拿它算出来的任何数字都是对着错的颜色算的。robot.ps1 直接拒绝。
